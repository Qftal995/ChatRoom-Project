<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.book.java_chatroom.mapper.MessageMapper">
    <select id="getLastMessageBySessionId" resultType="java.lang.String">
        select content from message where sessionId = #{sessionId} order by postTime desc limit 1
    </select>
    
    <select id="getMessagesBySessionId" resultType="com.example.book.java_chatroom.model.Message">
        select m.messageId, m.fromId, u.username as fromName, m.sessionId, m.content, m.postTime
        from message m
        left join user u on u.userId = m.fromId
        where m.sessionId = #{sessionId}
        order by m.postTime asc
    </select>

</mapper>